# ------------------------------
# Conda section
#
# Install these first with:
#   conda install -c conda-forge -c pytorch --file requirements.txt
# ------------------------------

python>=3.10
numpy
# opencv          # provides cv2! NOOOO, use opencv-python-headless instead
pillow            # PIL
requests
scipy             # spatial.distance, ndimage, etc.
scikit-learn      # KMeans, GaussianRandomProjection
matplotlib        # pyplot, cm
PySide6           # Qt GUI
faiss-cpu         # fast nearest neighbor search (optional)
tqdm              # progress bars
streamlit         # web UI
pytorch           # from pytorch channel (CUDA/CPU build as needed)
torchvision       # match the PyTorch version

# ------------------------------
# Pip section
#
# After activating the conda environment, run:
#   pip install -r requirements.txt --extra-index-url https://pypi.org/simple
# to install anything not (reliably) available via conda.
# ------------------------------

opencv-python-headless
#ultralytics                # YOLOv8 implementation (optional)
#streamlit-image-coordinates # streamlit image click plugin (optional)
#pandas                      # for dataframes (optional, used in some scripts)

# ACHTUNG: Wenn torch ohne GPU-Support installiert wurde:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

